<%- include('../layout-header', { title: 'Import vCard' }) %>

<h1>Import vCard Contacts</h1>

<div class="form-card">
  <p>Upload a vCard (.vcf) file to import phone numbers and emails. Contacts will be automatically matched against your existing contacts by name.</p>
  <form method="POST" action="/realestate/import-vcard/upload" enctype="multipart/form-data">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <div class="form-group">
      <label for="vcffile">vCard File (.vcf)</label>
      <input type="file" id="vcffile" name="vcffile" accept=".vcf" required>
    </div>
    <p style="font-size: 0.85rem; color: #64748b; margin-bottom: 1rem;">
      The file will be parsed and matched against your contacts. Mobile/cell phone numbers are preferred. You can review all matches before applying them.
    </p>
    <button type="submit" class="btn btn-primary">Upload & Match</button>
  </form>
</div>

<a href="/realestate" class="btn btn-link">Back to Real Estate</a>

<%- include('../layout-footer', { scripts: [] }) %>
